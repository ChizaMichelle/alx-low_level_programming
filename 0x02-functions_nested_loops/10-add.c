#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @b: the first number to be computed
 * @c: the second number to be computed
 * Return: Integer value
 */

int add(int b, int c)

{
return (b + c);
}
